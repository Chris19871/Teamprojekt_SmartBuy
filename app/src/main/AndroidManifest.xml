<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="smartbuy.teamproject.application">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/smartbuy_logo"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings"></activity>
        <activity
            android:name=".EinkaufslisteActivity"
            android:label="@string/title_activity_einkaufsliste"></activity>

        <activity
            android:name=".EinkaufmodusActivity"
            android:label="@string/title_activity_einkaufmodus"></activity>
    </application>
</manifest>
